[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    golden: F.6.5 Brain golden case regression tests
    slow: tests that take >5s (excluded from quick runs)
filterwarnings =
    ignore::DeprecationWarning
