/* Hermes 2.0 — Config pública (sem secrets).
   Token lido do localStorage em runtime por api.js.
   HERMES_API aponta para Tailscale IP da VPS — só acessível na rede Tailscale. */
window.HERMES_API = 'http://100.74.227.37:8800';
window.HERMES_VERSION = '2.0';
