"""hermes-skills custom MCP — F.5.2 scaffold.

HYBRID storage:
- YAML reads/writes (glob skills/*.yaml) — list, get, toggle_active
- DB stubs in-memory (F.4 entrega skill_proposals table persistence)
- skill_runs table read SE existe (best-effort metrics)
"""
