fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
python-dotenv>=1.0.0
pystray>=0.19.0
pywebview>=5.0
Pillow>=10.0
pydantic>=2.0
pydantic-settings>=2.0
python-telegram-bot>=21.0
websockets>=12.0
psutil>=5.9.0
slowapi>=0.1.9
# F.5.1 — ContextForge gateway scaffold (FastMCP 3.0 framework picked over IBM ContextForge)
# Rationale: FastMCP 3.0 GA Feb 2026, stable 3.4.2 jun, 4M dl/day, OAuth+OTel built-in,
# simple pip install. IBM ContextForge 1.0.0 GA Feb 2026 (Docker proxy separado) defer F.5.6+.
fastmcp>=3.4.0,<4.0
PyYAML>=6.0
# F.6.1 — Brain orchestrator state machine (transitions lib, lightweight FSM)
# Rationale (D1): plain Python asyncio + transitions FSM picked over LangGraph
# (heavy deps), OpenAI Agents SDK (proprietary), CrewAI (multi-agent overkill).
transitions>=0.9.0
# F.6.5 — Golden cases test harness (pytest + parallel + YAML schema)
# pytest>=9.0 + PyYAML>=6.0 already pinned above; xdist + asyncio added F.6.5.
pytest>=9.0
pytest-asyncio>=1.4
pytest-xdist>=3.6
# F.9.2 — Pipeline Studio execution engine (Jinja2 strict templating variable substitution)
# Rationale (D3): StrictUndefined raises on missing var → fail-fast, prevents silent corruption
# em owner pipelines F.7 cobaia. SandboxedEnvironment blocks arbitrary attr access.
jinja2>=3.1
